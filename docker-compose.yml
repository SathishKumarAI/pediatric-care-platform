services:
  backend:
    build: .
    ports:
      - "8000:8000"
    environment:
      PROVIDER: ${PROVIDER:-ollama}
      # Point at a Neo4j service to enable the graph backend (optional).
      # NEO4J_URI: bolt://neo4j:7687
    # Uncomment to add the knowledge-graph store:
    # depends_on: [neo4j]

  # neo4j:
  #   image: neo4j:5
  #   environment:
  #     NEO4J_AUTH: neo4j/please-change-me
  #   ports: ["7474:7474", "7687:7687"]
